{
  "name": "academy-portal",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "nuxt": "^3.17.7",
    "vue": "^3.5.16"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  }
}
